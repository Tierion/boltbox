/**
 * @file This is meant as a placeholder file. When running this with your own project
 * you will want to rename to `index.js` and replace
 * with your own custom configs
 */

module.exports = require('boltwall').TIME_CAVEAT_CONFIGS
